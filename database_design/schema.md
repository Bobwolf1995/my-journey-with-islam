# تصميم قاعدة البيانات - رحلتي مع الإسلام

هذا الملف يوضح التصميم العام لقاعدة بيانات منصة "رحلتي مع الإسلام".  
الهدف هو بناء قاعدة بيانات منظمة، قابلة للتوسع، وتدعم التطبيق، لوحة الإدارة، الدورات، المهام، المرشدين، المكتبة، الدفع، والأوسمة.

## قاعدة البيانات المقترحة

يفضل استخدام:

```text
PostgreSQL